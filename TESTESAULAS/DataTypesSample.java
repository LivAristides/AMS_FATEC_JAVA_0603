package TESTESAULAS;

public class DataTypesSample {
    public static void main(String args[])
    {
        //tipos de dados primitivos 
        //escalares (definem variaveis que armazenam apenas um valor)
        //tres categorias:
        // - numericas (byte, short, int, long, float, double)
        // - caracteres (char)
        // - lógico (boolean)

        int var_i = 0;
        long var_l = 0;

        System.out.println("\ninteger: " + var_i);
        System.out.println("\ninteger: = 4 bytes de tamanho => 32 bits" );
        System.out.println("\ninteger: " + Integer.MAX_VALUE);
        System.out.println("\ninteger: " + Integer.MIN_VALUE);


        System.out.println("\ninteger: " + var_l);
       System.out.println("\nDouble: = 8 bytes de tamanho => 64 bits" );
       System.out.println("\nDouble: " + Long.MIN_VALUE);
       System.out.println("\nLong: " + Long.MIN_VALUE);

       for (int 1=0; < 256; i++)
       {
        System.out.println(i + ""+ (char))
       }
    }
    
}
